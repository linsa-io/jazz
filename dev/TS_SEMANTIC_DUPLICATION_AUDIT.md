# TS semantic duplication audit

Investigation slice: read-mostly audit of the TypeScript runtime/worker layers. No source changes made.

## Part 1: TS semantic reimplementation sites

| Risk        | Site                                                                                                                                                                                                         | Duplicated semantics                                                                                                                                                                                                                                                                                                         | Why it exists                                                                                                                                          | Drift evidence / risk                                                                                                                                                                                                                                                                                                             | Core-side replacement path                                                                                                                                                                                                |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| High        | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:1135`-`1176`, `1179`-`1210`                                                                                                        | `applyTransactionReadOverlay` overlays pending writes, filters rows, sorts rows, and applies limit/offset in TS. `rowMatchesQuery` only supports `conditions[].op === "eq"`. `sortRowsForQuery` compares selected values and falls back to `id.localeCompare`.                                                               | Read-your-writes for uncommitted JS transactions: committed rows are read from core, then pending TS writes are merged above the engine.               | Already drifted: missing/default ordering was observed. The implementation is eq-only, does not execute the lowered query model, and must agree with core equality, null, bytes, array, id, ordering, and windowing rules.                                                                                                        | Replace with a native in-transaction read view or open-tx query API so normal core query lowering/evaluation handles pending writes. Delete the TS overlay evaluator.                                                     |
| High        | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:3650`-`3656`, `3659`-`3691`, `3694`-`3814`; `3869`-`4010`                                                                          | Relation/include materialization is rebuilt in TS: relation snapshot metadata stripping, root selection, edge grouping, nested array values, hidden/public include column aliases, `Optional`/`AtLeastOne`/`MatchCorrelationCardinality`, client limit/offset, incremental relation row/edge maintenance.                    | The worker receives core relation snapshots/deltas but still shapes them into app rows and maintains materialized relation subscription state locally. | High architectural risk because include semantics, requirement filtering, row ordering, edge pruning, and metadata visibility must match core. Core already has relation snapshot materialization machinery (`crates/jazz/src/node/query_eval.rs:7309`-`7497`), so duplicated TS shaping is not the authoritative implementation. | Have core emit the requested materialized output shape or a stable relation-frame descriptor that TS only transports/decodes. For subscriptions, core should own relation edge/root materialization and semantic pruning. |
| High        | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:4184`-`4219`, `4350`-`4422`, `4434`-`4525`; `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:1213`-`1235` | TS computes semantic subscription deltas by comparing current vs previous rows, including identity/dedupe, index changes, value equality, projection to output columns, and update-vs-no-op decisions. `refreshOpenedPlainSubscriptions` refreshes opened subscriptions after boundary effects and emits TS-computed deltas. | Latency and local refresh around newly committed writes/edge refreshes; also frame production for worker-to-main delivery.                             | Risk is not just transport: `nativeDeltaFromRows` decides what is added/updated/removed and when an index move is an update. Equality uses TS value rules. If core delta semantics change, TS can emit different deltas.                                                                                                          | Core should produce authoritative deltas/resets for semantic changes. TS can reduce a documented protocol stream but should not compute query deltas from row sets.                                                       |
| Medium-high | `packages/jazz-tools/src/runtime/subscription-manager.ts:52`-`69`, `72`-`131`, `133`-`185`, `233`-`360`, `362`-`393`                                                                                         | Main-thread subscription reducer normalizes delta streams, sorts changes by index, suppresses removes when a materialized add/update for the same id exists, removes duplicate ids before inserting, and bulk-merges placements.                                                                                             | Maintains materialized client arrays and optimizes large deltas.                                                                                       | Some reducer behavior is legitimate protocol application, but it makes semantic choices where the wire contract must be exact: same-id add/remove ordering, duplicate ids, index clamping, and reset materialization. It must track core's delta contract exactly.                                                                | Keep only a minimal protocol reducer whose behavior is specified by core wire semantics. Prefer reset/full-state frames for ambiguous coalesced cases, or move coalescing decisions into core.                            |
| Medium-high | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:2699`-`2757`, `2783`-`2844`; `3127`-`3198`                                                                                         | TS parses/recognizes relation IR and lowers/coerces predicates: unwraps `TableScan`/`Filter`/`Project`/`OrderBy`/`Limit`/`Offset`, rewrites `In` on arrays into `Any` of `Eq`/`Contains`, coerces literals by schema, and lowers predicates to native filters.                                                               | Bridge from TS query builder JSON/relation IR into the current wasm/native query API.                                                                  | This is translation rather than evaluation, but it is still semantic. If core supports a predicate/operator/query-shape that TS unwrap rejects or rewrites differently, behavior diverges before the query reaches core.                                                                                                          | Expose a core API that accepts the canonical query IR directly, or share generated wire/query codecs so TS serializes without semantic rewriting.                                                                         |
| Medium      | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:2388`-`2443`, `4232`-`4348`, `4409`-`4422`                                                                                         | TS derives output projection columns, detects identity projections, filters internal/hidden columns, repacks raw native records, and supplies missing array/null values for frame rows.                                                                                                                                      | Worker-to-main frame format needs output-column descriptors; packed reset pass-through must avoid leaking internal fields.                             | Previous perf slice found a real guard bug: plain table queries do not have `outputColumns === null`; `subscriptionOutputColumns` always builds full public output columns. Raw engine records can include internal/provenance/meta fields, so output shaping in TS has correctness risk.                                         | Core should emit rows already projected to the requested public output descriptor, or include a descriptor that the main thread can filter against without TS guessing projection identity.                               |
| Medium      | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:3659`-`3691`, `4232`-`4348`                                                                                                        | Internal-vs-public field classification and metadata stripping is duplicated in TS.                                                                                                                                                                                                                                          | Relation snapshots and packed reset pass-through can carry engine/internal fields that are not app-schema columns.                                     | Drift or omission can leak internal fields or drop legitimate user columns with names overlapping metadata; this was a correctness issue in the packed reset pass-through slice.                                                                                                                                                  | Core should own app-visible projection. If frames remain raw, main-thread decode must be schema/output-column filtered by a core-provided descriptor.                                                                     |
| Medium      | `packages/jazz-tools/src/runtime/db.ts:1627`-`1653`                                                                                                                                                          | `subscribeAll` computes output table, include output, selected columns, native subscription columns, translates the query, and transforms wasm rows into typed output rows.                                                                                                                                                  | Public TS API ergonomics and typed row shaping.                                                                                                        | Mostly presentation/codec work, but selecting output table/includes/projection overlaps query output semantics and relation traversal behavior. Drift risk grows when includes/relations add new output shapes.                                                                                                                   | Keep type hydration in TS, but have core return a stable logical output schema/descriptor for the query instead of recomputing it in TS.                                                                                  |
| Medium-low  | `packages/jazz-tools/src/runtime/db.ts:216`-`235`                                                                                                                                                            | `limitQueryToOne` mutates the built query JSON to set `limit = 1` for `one()`.                                                                                                                                                                                                                                               | API sugar for fetching one row through the same native query entry.                                                                                    | Windowing itself is core-owned. This is acceptable only if it is treated as query construction, not post-read slicing; it depends on core default ordering for deterministic `one()`.                                                                                                                                             | Prefer a native `one` query API that applies core planning, ordering, and limit semantics directly, or keep this as builder-level syntax only.                                                                            |
| Medium-low  | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:2446`-`2465`                                                                                                                       | `addNestedOuterColumns` mutates nested subquery projections to include outer correlation columns needed later by materialization.                                                                                                                                                                                            | TS relation materializer needs hidden correlation data to rebuild array subqueries.                                                                    | Projection mutation is semantic because it changes the requested shape internally and must later hide the extra columns.                                                                                                                                                                                                          | Core relation planner should own correlation-column plumbing and hide implementation fields before transport.                                                                                                             |
| Low-medium  | `packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:602`-`655`                                                                                                                         | Permission/visibility checks are not reimplemented for `canInsert`/`canRead`/`canUpdate`/`canDelete`; TS encodes inputs and calls native dry-run APIs.                                                                                                                                                                       | Public API boundary.                                                                                                                                   | No duplicate runtime policy evaluator found in the audited TS worker/adapter/db paths. Residual risk is indirect: TS overlays and materializers operate on rows already returned by core, but pending transaction reads can expose pending writes before core evaluates the whole query/policy view.                              | Keep policy decisions in core; make in-tx reads evaluate policies against the same core read view as ordinary reads.                                                                                                      |
| Low-medium  | `packages/jazz-tools/src/runtime/db.ts:208`-`214`                                                                                                                                                            | `nativeDbQueryOptions` strips `subscriptionMode` before passing read options to native.                                                                                                                                                                                                                                      | Runtime routing detail; subscription mode is TS client behavior, not a core read option.                                                               | Low semantic risk, but option translation should stay mechanical and explicit so TS-only flags do not silently affect core reads.                                                                                                                                                                                                 | Keep as transport/API option separation.                                                                                                                                                                                  |

Not counted as semantic duplication: postcard/native-row byte packing, ArrayBuffer transfer, and typed object hydration are transport/presentation work when they do not decide membership, order, identity, visibility, or projection. The problem cases above cross that line.

## Part 2: shortest path to native in-transaction reads

### What `ReadViewSpec` can express today

`ReadOpts` already carries `read_view: ReadViewSpec` as the semantic read view for one-shot reads (`crates/jazz/src/db.rs:5898`-`5911`). `ReadViewSpec` has a source, optional schema lens, and local overlays (`crates/jazz/src/protocol.rs:1253`-`1274`). Sources are `Current`, `Branch`, `MergedBranches`, and `Snapshot` (`crates/jazz/src/protocol.rs:1344`-`1376`). Overlays include `DirectBatch`, `AcceptedTransaction`, and `OpenTransaction` (`crates/jazz/src/protocol.rs:1402`-`1422`).

Execution support is narrower. `ensure_supported_read_view` accepts `Current`, and accepts a branch read view only when schema is default and overlays are empty (`crates/jazz/src/db.rs:6103`-`6113`). The query lowering path has the same shape: `query_read_set_for_read_view` supports current and branch-without-overlays, and rejects merged branches, snapshots, and branch overlays/lenses (`crates/jazz/src/node/query_eval.rs:2093`-`2142`). The wasm boundary rejects any provided `read_view`/`readView` before parsing options (`crates/jazz-wasm/src/lib.rs:2226`-`2268`).

There is already lower-level engine machinery for open transaction overlays. `tx_query_read_set` builds a read set with `SourceExpr::WithOverlays` and `OverlayRef::OpenTransaction(tx_id)` (`crates/jazz/src/node/query_eval.rs:1997`-`2048`). Source resolution accepts exactly an open transaction overlay on visible current/snapshot input (`crates/jazz/src/node/query_eval.rs:616`-`642`) and materializes it with `node.tx_current_rows(tx_id, table)` (`crates/jazz/src/node/query_eval.rs:850`-`864`). `NodeState::tx_query` evaluates a validated query inside an open exclusive transaction, records the predicate read, then applies core ordering/window finishing (`crates/jazz/src/node/query_eval.rs:7727`-`7760`).

### Where pending writes are authoritative today

The TS adapter is authoritative for public transaction IDs and read overlays. `beginTransaction` creates a `pendingTxs` entry with `writes: []` (`packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:657`-`660`). Transactional insert/update/upsert/delete paths stage writes both into the wasm transaction object and into `tx.writes` as `PendingTxWrite` rows/base/deletes (`packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:455`-`460`, `487`-`492`, `515`-`524`, `547`-`570`, `587`-`592`). `applyTransactionReadOverlay` then evaluates the read against committed rows plus that TS mirror (`packages/jazz-tools/src/runtime/native-runtime/native-runtime-adapter.ts:1135`-`1176`).

The wasm transaction object is not currently an open core transaction view. `WasmTx` stores `writes: Option<Vec<WasmTxWrite>>` in JS/Wasm memory (`crates/jazz-wasm/src/lib.rs:1049`-`1054`). `mergeableTx`, `mergeableTxForIdentity`, and `exclusiveTx` initialize that vector (`crates/jazz-wasm/src/lib.rs:1653`-`1679`). Transaction write methods append to it (`crates/jazz-wasm/src/lib.rs:1737`-`1823`), and `commit` consumes the vector and only then calls core commit helpers (`crates/jazz-wasm/src/lib.rs:1825`-`1847`). The returned `WasmWrite` carries a committed/committing `tx_id` and write-state handle (`crates/jazz-wasm/src/lib.rs:2184`-`2224`), but there is no wasm read entry that evaluates against the uncommitted vector.

So the shortest path is not just "pass `read_view` through wasm." Core has an open-exclusive transaction query path, but the current wasm API buffers pending writes outside that open-tx storage.

### Shortest viable implementation path

1. **Plumb an open transaction handle through wasm/napi.** Add a wasm transaction mode that opens a real core exclusive transaction immediately, keeps its `OpenTxId`/public tx id accessible, and applies each transactional write into core staging as it occurs. File-level targets: `crates/jazz-wasm/src/lib.rs` transaction constructors and `WasmTx` methods; matching napi/native runtime types if a node path exists outside wasm.

2. **Expose one-shot reads against that open transaction.** Add a wasm read method such as `allInTransaction(prepared, txHandle, opts)` or support a `read_view` with `OpenTransaction` only when it resolves to an open local tx. Internally call the existing core `tx_query` path for exclusive transactions rather than `Db::all` plus TS overlay. File-level targets: wasm read methods at `crates/jazz-wasm/src/lib.rs:1111`-`1124` and core `NodeState::tx_query` at `crates/jazz/src/node/query_eval.rs:7727`-`7760`.

3. **Make `ReadViewSpec` execution match the public model.** Longer-lived API shape should allow a read view like current source plus `overlays: [OpenTransaction { tx }]`. The query read-set resolver already has an overlay abstraction (`crates/jazz/src/node/query_eval.rs:616`-`642`), but public read-view execution currently rejects overlays (`crates/jazz/src/db.rs:6103`-`6113`; `crates/jazz/src/node/query_eval.rs:2115`-`2142`; wasm rejects at `crates/jazz-wasm/src/lib.rs:2258`-`2268`). This is mostly plumbing once open txs are stored in core rather than wasm-local vectors.

4. **Delete the TS overlay evaluator.** After the native read path exists, `NativeRuntimeAdapter.query()` can pass the transaction/read-view handle to core and remove `applyTransactionReadOverlay`, `rowMatchesQuery`, `sortRowsForQuery`, and the query-only TS comparison functions. The black-box contract should be "same query, same transaction, same rows/order as core."

Missing core/API pieces:

- Public wasm/napi transaction handles backed by core open transaction state, not `Vec<WasmTxWrite>` buffered in wasm memory.
- A supported public read option or method for current plus open-transaction overlay.
- Policy/read authorization evaluation for in-tx queries through the same core query program path as ordinary reads. `tx_query` currently uses `AuthorId::SYSTEM` in the call shown at `crates/jazz/src/node/query_eval.rs:7737`-`7743`; public session-aware transaction reads need the caller identity.
- Commit/rollback wiring from the new handle to existing exclusive/mergeable commit semantics, plus conflict/predicate-read preservation.
- TS adapter transaction bookkeeping simplified to lifecycle/transport only, with pending row mirrors deleted once read-your-writes is native.

Rough estimate:

- Plumbing-only prototype for exclusive transactions: 2-3 focused days if limited to one-shot `all`/`one` and local wasm memory/browser paths.
- Product-ready core/API path: 1-2 weeks to cover identity-aware reads, napi parity, error/lifecycle semantics, tests, and removal of TS overlay code.
- Relation subscriptions/read views over transaction overlays are a separate phase unless the immediate goal is only in-transaction one-shot reads.

## Draft SPEC open question

Proposed location: `crates/jazz/SPEC/13_db_api.md`. This is an API boundary principle more than a query-language feature; `6_queries.md` can cross-reference it.

> Open question: where may non-core layers evaluate query semantics?
>
> Principle: query semantics live in the core. TypeScript, wasm, napi, and runtime client layers may build queries, serialize query IR, cache prepared handles, transport frames, and hydrate typed application objects, but they must not independently evaluate predicates, ordering, limit/offset/windowing, relation/include membership, permission visibility, identity/dedupe, or semantic delta coalescing. When a client API needs read-your-writes or another alternate read view, the API should express that read view to core and let the normal lowered query execute there. Any TS-side reducer must be a specified wire-protocol reducer, not a second query engine.

## Tooling friction

A wasm/napi public API that exposes core `tx_query` for local tests would have made this audit shorter and would remove the current need to reason about two transaction staging layers.
